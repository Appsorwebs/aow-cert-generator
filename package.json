{
  "name": "aow-cert-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "build": "esbuild assets/js/*.js --bundle --minify --outdir=assets/dist --format=iife --sourcemap",
  "dev": "esbuild assets/js/*.js --bundle --sourcemap --outdir=assets/dist --format=esm --watch"
  },
  "devDependencies": {
    "esbuild": "^0.18.0"
  }
}
